import React from 'react';

const User = ({ name, email }: {
    name: string,
    email: string,
    // deleteUser: () => deleteUser()
}) => {


    return (
        <div>
           
        </div>
    );
};

export default User;